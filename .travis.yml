language: python

# Worry about Python 3.8 later
python:
    - "3.6"

# TODO : flake8/linting, 
install:
    - pip install --upgrade pip && pip install -r requirements.txt
    - python3 setup.py

script:
    - pytest -vv
